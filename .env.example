# Export these variables in the shell before running a pipeline.
# The scripts use Google Colab-style defaults when variables are absent.
CIC_UNSW_RAW_CSV=/path/to/CIC-UNSW-NB15/CICFlowMeter_out.csv
CIC_BASE_DIR=/path/to/experiment_outputs
BCCC_CSE_CIC_IDS2018_ROOT=/path/to/BCCC-CSE-CIC-IDS2018
